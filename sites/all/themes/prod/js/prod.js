(function ($) {
  Drupal.behaviors.prod = {
    attach: function (context, settings) {


      // куки в списке магазинов -----------------------------------------------
      // $('.page-production-queries').once(function() {
      //   $('a.arch').on('click', function () {
      //     var id = $(this).data('id');
      //     if ($(this).hasClass('visible')) {
      //       $(this).removeClass('visible');
      //       $('.arch-' + id).removeClass('visible');
      //     } else {
      //       $(this).addClass('visible');
      //       $('.arch-' + id).addClass('visible');
      //     }
      //   });
      // });

    }
  };
})(jQuery);
